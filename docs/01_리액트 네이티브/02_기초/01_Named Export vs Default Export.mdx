
## 예제

```tsx
// Named export - 정확한 이름 필요
import { AppNavigator, HomeScreen } from 'shared';

// Default export - 아무 이름이나 가능
import MyNavigator from 'shared'; // 혼란 가능성
```

라이브러리나 shared 패키지에서는 named export 쓰는게 좋음