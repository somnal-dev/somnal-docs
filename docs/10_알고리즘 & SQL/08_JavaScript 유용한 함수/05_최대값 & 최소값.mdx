export const arr = [1, 100, 2, -202, 33];

## 최대값 찾기

```js
const arr = [1, 100, 2, -202, 33];
const max = Math.max(...arr);
```

결과 : {Math.max(...arr)}

## 최소값 찾기

```js
const arr = [1, 100, 2, -202, 33];
const min = Math.min(...arr);
```

결과 : {Math.min(...arr)}

## 최대값 인덱스 찾기

```js
const arr = [1, 100, 2, -202, 33];
const maxIdx = arr.indexOf(Math.max(...arr));
```

결과 : {arr.indexOf(Math.max(...arr))}

## 최소값 인덱스 찾기

```js
const arr = [1, 100, 2, -202, 33];
const minIdx = arr.indexOf(Math.min(...arr));
```

결과 : {arr.indexOf(Math.min(...arr))}
