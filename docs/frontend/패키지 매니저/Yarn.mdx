---
sidebar_position: 2
---

2017년에 Facebook 에서 만든 npm 대체 패키지 매니저.<br/>
npm 의 보안문제를 해결하여 더 안정성을 갖고 있음.

### 장점

npm 대비 빠른 속도 (여러개의 패키지를 병렬로 설치하므로 빠름)

### 설치 방법
```bash
npm install -g yarn
```

혹은 brew 로 설치 가능
```bash
brew install yarn
```
